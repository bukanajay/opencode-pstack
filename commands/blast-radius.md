---
description: Find what a change could break somewhere else before it ships, beyond the diff, and prove the one fact it's safe because of by running real 
agent: build
---
Load the `blast-radius` skill with the skill tool and follow it end to end.
Prove the one safety fact by running real code, not by asserting it.
User request: $ARGUMENTS
